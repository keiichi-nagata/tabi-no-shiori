# 旅のしおり作成アプリ 実装指示書

## 1. 概要

日程・人数・行きたい場所を入力すると、AIが旅行プランを5案提示し、選択・手動編集を経て「旅のしおり」を完成させるWebアプリ。完成後はPINコード付きの共有URLで家族・同行者と共有でき、旅行中も編集可能。作成したしおりはアプリ内に旅行履歴として保存される。

対象ユーザー：個人・家族旅行の計画者。機能は多機能化させず、下記フローの一本道を丁寧に作り込むことを最優先とする。

---

## 2. 全体フロー

```
① 入力
  └ 日程（開始日・終了日）
  └ 人数（大人／子ども、子どもは年齢も入力）
  └ 行きたい場所・お店（自由入力、複数可）
       ↓
② AIがプラン5案を提示
  └ 各案：タイトル／テーマ／徒歩距離・移動回数・混雑度の簡易指標
  └ 入力した場所に加え、AIが周辺のおすすめスポットを提案（要区別表示）
       ↓
③ 選択 → 手動編集
  └ 1案を選択
  └ スポットの追加・削除・時間調整
  └ スポット間に「移動ブロック」を追加（手段／所要時間／メモ：列車番号や乗換情報など）
       ↓
④ AIがしおり完成版を整形
  └ 手動編集内容をもとに、体裁を整えた最終版を生成
       ↓
完成後
  └ PINコード発行 → 共有URL＋PINで共有
  └ 旅行中も編集可能（スポットごとに感想メモを追記できる）
  └ 旅行履歴としてアプリに保存（過去のしおり一覧から閲覧可能）
```

---

## 3. 画面要件

### 3-1. 入力画面（①）
- 目的地：都道府県・エリア名を入力（必須。複数地域可）
- 日程：開始日・終了日（カレンダーUI）
- 到着・出発予定時刻：**初日の到着予定時刻**、**最終日の出発予定時刻**を入力（時刻指定、または「午前／午後／夜」程度のざっくり選択でも可）。中日（2日目以降〜最終日前日）は指定不要
- 人数：大人の人数、子どもの人数＋年齢（複数人分）
- 行きたい場所・お店：フリーテキストで複数追加できるタグ入力（任意。目的地の中で具体的に行きたいスポットがあれば入力する位置づけ）
- 送信すると②のプラン提案画面へ

### 3-2. プラン提案画面（②）
- 入力内容のサマリーをチップ表示（日程／人数／行きたい場所）
- 5案をカード形式で縦に並べる。各カードに以下を含む：
  - タイトル（例：「王道の京都と大阪グルメを両立」）
  - 一言テーマ／タグ（例：「王道・バランス型」「子ども連れ・省エネ型」）
  - 簡易指標：徒歩距離／移動回数／混雑度
  - スポットタグ一覧（入力したスポットと、AI提案のスポットを視覚的に区別。例：AI提案は✦マークや色を変える）
  - 「このプランを選ぶ」ボタン
- 5案の切り口の例：王道バランス型／子ども連れ省エネ型／グルメ重視型／定番×穴場ミックス型／弾丸満喫型（子どもの年齢や人数に応じて内容を調整）

### 3-3. 選択・編集画面（③）
- 選んだプランのスポット一覧を時系列で表示（編集可能なタイムライン形式）
- 各スポット：時間／名称／メモを編集可能。追加・削除ができる
- スポットの並べ替えはドラッグ&ドロップで行う（各スポットにドラッグハンドルを表示）
  - 並べ替えを行うと、影響を受ける移動ブロックの内容（手段・所要時間・メモ）は自動的に未設定状態にリセットし、再入力を促す（「移動手段を選択してください」という空欄表示＋ガイド文言を表示）
- スポットとスポットの間に「移動ブロック」を追加できる
  - 移動手段（徒歩／電車／バス／車 などから選択、または自由入力）
  - 所要時間
  - メモ欄（列車番号、乗り換え情報など自由記述）
- 「しおりを完成させる」ボタンでAIに整形依頼

### 3-4. 完成しおり画面（④・完成後）
- 表紙：タイトル、日程、人数
- 日ごとにセクション分け。各セクションにタイムライン形式でスポート→移動→スポットの順に表示
- 各スポットに「感想メモ」欄（旅行中に追記できる。既存メモと視覚的に区別する見た目にする）
- 持ち物・共通メモ欄
- 共有セクション：共有URL表示、PINコード表示、コピー機能
- 「旅行履歴に保存済み」のステータス表示

### 3-5. 共有・閲覧画面
- URLアクセス時、まずPINコード入力画面を表示
- PIN照合後にしおり内容を表示（サーバー側でPIN照合してからデータを返す設計。PIN不一致時はデータ自体を返さない）
- PIN一致後は、旅行中の感想メモ追記や移動メモの編集も可能（編集権限はPINを知っている人全員に付与するシンプルな設計）

### 3-6. 履歴画面
- ログイン中ユーザーが作成したしおりの一覧
- 作成日・行き先・日程を一覧表示し、タップで完成しおり画面へ遷移

---

## 4. データモデル（例）

- `users`：id, email, created_at（簡易メール認証程度）
- `itineraries`：id, user_id, title, destination（目的地。複数可の場合はjson配列）, start_date, end_date, arrival_time（初日到着予定）, departure_time（最終日出発予定）, adults, children(json: 年齢配列), created_at, pin_hash
- `days`：id, itinerary_id, date, theme
- `spots`：id, day_id, order, time, name, note, memo（旅行中の感想）, is_ai_suggested（bool）
- `transits`：id, day_id, order, mode（徒歩／電車／バス／車等）, duration, note（列車番号等）

スポットと移動（transit）は同じ`day_id`配下で`order`によって表示順を制御し、タイムライン上で交互に描画する。

---

## 5. AI呼び出し仕様

### 5-1. ②プラン5案生成
- 入力（目的地・日程・初日到着予定時刻・最終日出発予定時刻・人数・子どもの年齢・行きたい場所）をもとに、5案をJSON形式で出力させる
- 出力させる項目：`title`, `theme_tag`, `description`, `walk_level`, `move_count`, `congestion`, `spots`（`name`, `is_ai_suggested`）
- 初日は到着予定時刻より前、最終日は出発予定時刻より後にスポットを組み込まないようプロンプトで制約する（例：到着が14時なら初日午前のスポットは提案しない）
- 子どもの年齢が低い場合は「徒歩距離少なめ」「移動回数少なめ」の案を優先的に含めるようプロンプトで指示する
- 入力されていない周辺スポットも積極的に提案し、`is_ai_suggested: true`でフラグを立てる

### 5-2. ④しおり整形
- 選択したプラン＋ユーザーの手動編集内容（スポット・移動ブロック）を渡し、体裁を整えた最終版（日ごとのタイムライン構造のJSON）を生成させる
- ここでは新しいスポットを勝手に追加させず、ユーザーが確定した内容を整形・言い回しの調整に留める指示にする

---

## 6. 共有・セキュリティ要件

- PINコードは4桁。作成時に自動発行（画面上でランダム表示、必要であれば手動変更も可）
- PINはハッシュ化してDB保存し、平文では保持しない
- PIN照合の試行回数制限を設ける（例：5回失敗で10分ロック）
- 共有URLとPINは別チャネルで伝える運用を前提とし、UI上にもその旨を案内文として表示する

---

## 7. デザイントーン

- コンセプト：パスポート／旅行ジャーナル風
- 配色：ネイビー（表紙・共有欄）、生成りのペーパー（本文背景）、ブラス（金）のアクセント、判子のような朱赤（スタンプ・感想メモの強調）
- フォント：見出しに明朝体（Shippori Mincho / Noto Serif JP）、本文にゴシック体（Zen Kaku Gothic New）
- タイムラインは縦のライン＋ドットで表現し、移動ブロックはスポットより控えめなトーンで挟み込む
- 感想メモは付箋風の見た目（背景色を変え、わずかに傾ける）にして、旅行中に追記したことが視覚的にわかるようにする

---

## 8. 技術スタック（推奨）

- フロントエンド／バックエンド：Next.js（App Router）
- AI呼び出し：Anthropic API（Claude）
- DB：Supabase や PlanetScale など、個人開発で運用しやすいマネージドDB
- 認証：メールリンク認証程度の簡易実装（履歴保存のためのみ使用）
- 並べ替えUI：`dnd-kit`などのドラッグ&ドロップライブラリ
- デプロイ：Vercel

---

## 9. 実装フェーズ案

1. 入力画面（①）と、AIへのプロンプト送信〜5案表示（②）までを最短で疎通させる
2. プラン選択→手動編集画面（③）を実装（移動ブロックの追加・編集を含む）
3. AI整形によるしおり完成（④）と表示画面を実装
4. 共有URL＋PIN機能を実装
5. 旅行中の感想メモ編集機能を追加
6. ログイン・履歴保存機能を追加

まずは1〜3（入力からしおり完成まで）を通しで動く最小構成として実装し、そのあとに共有・PIN・履歴を追加する順番を推奨する。
