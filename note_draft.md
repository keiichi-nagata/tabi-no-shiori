# 旅のしおりアプリの入手・公開ガイド＆Claude Codeカスタマイズ手順

日程と行きたい場所を入れると、AI（Claude）が旅行プランを5案つくってくれて、手直ししたものを「PIN付きの共有URL」で同行者に配れる——そんな無料の旅のしおりアプリを、自分のスマホ用に公開する手順と、Claude Codeで好みに改造する方法をまとめました。ひとり旅から友人同士の旅行、家族旅行まで、旅程を組む人なら誰でも使えます。

**この記事の目次**

- 1. はじめに（ここまで無料）
- 2. 【スマホ対応】自分専用アプリとしてGitHub Pagesで公開する手順（Claude Code不要）
- 3. 【応用】Claude Codeを使って自分好みに改造する手順
- 4. よくあるエラーとトラブルシューティング
- 5. 免責事項・注意事項

※ note上では見出しへのリンクジャンプが効かない場合があります。その際はブラウザのページ内検索（Ctrl+F / スマホは「ページ内を検索」）で見出し番号を探してください。

---

## 1. はじめに（ここまで無料）

### このアプリで解決できること

旅行の計画、地味に大変ですよね。ひとり旅でも、友人との旅行でも、家族旅行でも、こんな場面があると思います。

- 行き先の候補は挙がるのに、どういう順番で回れば効率がいいのか分からない
- 「移動にどれくらいかかるか」が読めず、1日に詰め込みすぎて後悔する
- せっかく作った旅程を、同行者にどう共有するか（印刷？スクショ？チャットに貼る？）
- 現地で「次どこだっけ」「集合何時だっけ」と何度も確認することになる
- ひとり旅だと、そもそも旅程を組むのが面倒で行き当たりばったりになる

このアプリは、その一本道を丁寧になぞります。

1. **入力**：目的地・日程・人数（子ども連れなら年齢も）・行きたい場所・泊まる宿・出発地/到着地を入れる
2. **AIが5案提案**：「王道バランス型」「子ども連れ省エネ型」「グルメ重視型」「定番×穴場ミックス型」「弾丸満喫型」など切り口の違う旅程を、実在のスポット名で5つ。各案に「かんたんスケジュール」と、AIが追加した周辺スポット（✦印）が付きます。同じ日のスポットは近い場所でまとめ、移動時間を考慮した順路になります
3. **選んで手直し**：タイムライン上でスポットの追加・削除・時刻調整・並べ替え・別の日への移動。スポットの間に「移動ブロック」（手段・所要時間・列車番号などのメモ）を挟めます。移動ブロックでは、前後のスポット名から **AIが所要時間の目安を見積もってくれます**（時刻表・渋滞は考慮しない概算。あくまで目安なので、ずれたら手入力で上書きできます）
4. **しおり完成**：表紙・日ごとのタイムライン・持ち物メモがついた1冊に
5. **共有**：4桁のPIN付きURLを発行。同行者はそのURLとPINでしおりを見られて、旅行中は各ブロックに「メモ」を書き足せます
6. **履歴**：作ったしおりはアカウントに保存され、あとから一覧で開けます

### 動作イメージ

（画像1：入力画面。目的地・日程・人数・行きたい場所などを入れる。ここがいわば「設定画面」です）

（画像2：AIからの提案5案。テーマの違うプランが、実在スポット名＋かんたんスケジュール付きで並びます。行きたくないスポットは × で外せます）

（画像3：完成した「旅のしおり」（1泊2日の例）。日ごとにタイムラインが分かれ、スポット→移動→スポット→宿泊（チェックイン／アウト）→到着地、の順に並びます。各ブロックに旅行中のメモを書け、下部に共有URLとPINコードが出ます）

（画像4：共有された人が見る画面。PINコードを入力すると中身が表示されます。5回間違えると10分ロック）

（画像5：旅行履歴の一覧。過去に作ったしおりが並び、タップで開けます）

### この記事に含まれるもの

- アプリ本体（無料）の入手先と、**GitHub Pagesで自分専用に公開する全手順**
- 共有・ログイン・本物のAI提案を有効にするための、**Supabase と Anthropic（Claude API）の実際の操作画面・設定値**
- コピーしてそのまま使えるコマンドと設定値
- **Claude Codeで改造するための、このアプリ専用のプロンプト例**
- 実際に私がつまずいたポイントとその解決策

Supabase・Anthropic・GitHubそれぞれの実際の操作、コピーしてそのまま使えるコマンドや設定値、そして実際につまずきやすかったポイントとその解決策まで、すべてこの記事の中だけで完結するように書いています。

### 必要なもの

このアプリの中心はAI（Claude）による旅程提案です。**その機能を使うには Supabase と Anthropic のアカウントが必須**で、GitHubだけでは画面のプレビューしかできません（提案はサンプルのテンプレート、データもそのブラウザ内だけ）。

| 目的 | 必要なもの | 費用 |
| --- | --- | --- |
| 画面と操作感をプレビューする | GitHubアカウント | 無料 |
| **AI提案・共有・履歴を実際に使う** | ＋ Supabaseアカウント、Anthropic APIキー | Supabaseは無料枠、Anthropicは従量課金（しおり1冊で数円〜十数円めやす） |
| Claude Codeで改造する | ＋ パソコン（Node.js 20以上）、Claude Code | Claude Codeの利用料 |

### 作業時間のめやす

- **2-1〜2-4（プレビュー公開まで）**：10〜15分。GitHubの画面をポチポチするだけ
- **2-5〜2-11（AI提案・共有まで有効化）**：さらに20〜30分（Supabase・Anthropicの登録を含む）
- **3章（Claude Codeで改造）**：環境構築に15分ほど。改造そのものは内容しだい

---

ここから先は有料エリアです。GitHubアカウントの作り方から、Supabase・Anthropicの設定画面、実際のコマンド、つまずきポイントの解決策まで、順を追って説明します。

---

## 2. 【スマホ対応】自分専用アプリとしてGitHub Pagesで公開する手順（Claude Code不要）

このアプリは「ソースコード」の状態で配布しています。公開に必要な「完成したファイル」は、あなたのGitHubにコピーして設定すると、**GitHub側が自動で組み立てて公開**してくれます。手順は2段階です。

- **2-1〜2-4（プレビューを公開）** … GitHubの画面操作だけ。パソコンにツールを入れる必要はありません。ただしこの段階では **AI提案はサンプル（テンプレート）** で、データもそのブラウザ内だけ、共有URLも同じブラウザでしか開けません。「画面と操作感を先に確認する」ためのステップです。
- **2-5〜2-11（実際に使えるようにする）** … Supabaseの画面操作に加えて、2-7 でだけパソコンのコマンドを数行打ちます（Node.jsが必要。Claude Codeは不要）。ここまでやると、Claudeによる本物のプラン提案・同行者との共有・端末をまたぐ履歴が使えます。

> **旅行の計画に実際に使うなら 2-5 以降まで必須**です。2-4 で止めると、旅程提案がサンプルのままになります。

### 2-1. GitHubアカウントを作る（持っている場合は飛ばしてOK）

1. パソコンかスマホのブラウザで <https://github.com/signup> を開きます。
2. メールアドレス・パスワード・ユーザー名を入力します。ユーザー名は公開URLの一部になります（例：ユーザー名が `taro` なら、あとで `https://taro.github.io/...` になります）。半角英数字とハイフンのみです。
3. 表示される簡単なクイズ（人間確認）に答えます。
4. 届いた確認メールのコードを入力すれば完了です。
5. プラン選択を聞かれたら「Free」で問題ありません。

すでにお持ちの方はこの手順は不要です。ログインして2-2の手順に進んでください。

### 2-2. 配布リポジトリを自分のアカウントにコピー（Fork）する

1. ブラウザで配布リポジトリを開きます：
   **<https://github.com/basel5freedom/tabi-no-shiori>**
2. 画面右上のほうにある **「Fork」** ボタンを押します。
3. 「Create a new fork」という画面が出ます。そのまま **「Create fork」** を押します（リポジトリ名は `tabi-no-shiori` のままにしておくと、あとの公開URLがきれいになります）。
4. 数秒待つと、`https://github.com/あなたのユーザー名/tabi-no-shiori` という自分専用のコピーができます。以降はこの「自分のリポジトリ」で作業します。

### 2-3. GitHub Pages（公開機能）を有効にする

1. 自分のリポジトリの上部メニューから **「Settings」**（設定・歯車アイコン）を開きます。
2. 左メニューの **「Pages」** をクリックします。
3. **「Build and deployment」** の **「Source」** を **「GitHub Actions」** に変更します（プルダウンから選ぶだけ。保存ボタンはありません）。
4. 次に、左メニューの **「Actions」→「General」** を開きます。
5. ページを一番下までスクロールし、**「Workflow permissions」** の項目で **「Read and write permissions」** を選び、**「Save」** を押します（この設定がないと、初回の公開が失敗します）。

### 2-4. 最初の公開を実行する

1. リポジトリ上部メニューの **「Actions」** タブを開きます。
2. 「I understand my workflows, go ahead and enable them」というボタンが出たら押します。
3. 左側の一覧から **「Deploy to GitHub Pages」** をクリックします。
4. 右側に出る **「Run workflow」** ボタンを押し、もう一度出てくる緑の **「Run workflow」** を押します。
5. 1〜3分で緑のチェックマークが付きます。
6. **「Settings」→「Pages」** に戻ると、上部に公開URL（`https://あなたのユーザー名.github.io/tabi-no-shiori/`）が表示されます。タップで開けます。

開いたアプリの上部には **「デモモード」** と表示されます。この状態では、旅程の5案は **Claudeではなくサンプルのテンプレート**（主要都市は実在スポット、それ以外は「〇〇城」などの一般名）で、移動時間のAI見積もりも使えず、保存先はそのブラウザの中だけ、共有URLも同じブラウザでしか開けません。**画面と操作の流れを確認するための状態**だと考えてください。

実際に旅行の計画に使うには、続けて **2-5 以降** を設定します（Claudeによる本物の提案・同行者との共有・履歴が有効になります）。

### 2-5. Supabaseプロジェクトを作る（データベースと認証）

Supabaseは、データベース・ログイン・サーバー処理をまとめて無料で使えるサービスです。

1. <https://supabase.com> を開き、**「Start your project」** → GitHubアカウントでサインインします。
2. ダッシュボードで **「New project」** を押します。
3. 初回は組織（Organization）の作成を求められます。名前は `personal` などでOK、プランは **Free** を選びます。
4. プロジェクトの設定を入力します。
   - **Project name**：`tabi-no-shiori` など任意
   - **Database Password**：「Generate a password」で生成し、**必ずどこかに控えておきます**
   - **Region**：`Northeast Asia (Tokyo)` を選ぶと日本から速いです
   - **GitHub (optional)**：接続不要です（このアプリはGitHub Pages側でビルドします）。
5. 画面下の **「Security」** にチェックボックスが3つあります。次のようにします（迷ったら**すべて初期設定のまま**でも問題ありません）。

   - **Enable Data API**（初期：ON）→ **ON のまま**（変更しない）
     アプリはブラウザから `supabase-js` でデータベースに直接アクセスします。OFF にすると画面が一切動きません。
   - **Automatically expose new tables**（初期：ON）→ **どちらでもOK**（ON のままで可）
     後で 2-6 で流す `schema.sql` が、必要なテーブルに必要な権限を**明示的に**付与します。ここが ON でも OFF でも、このアプリの動作は変わりません。「自分で管理したい」なら OFF でも構いません。
   - **Enable automatic RLS**（初期：OFF）→ **ON にするのがおすすめ**
     「新しく作ったテーブルに自動で行レベルセキュリティ（RLS）をかける」設定です。`schema.sql` は自前で全テーブルに RLS をかけるので今の動作は変わりませんが、ON にしておくと、あとで Claude Code でテーブルを追加したときに**保護を忘れて外部に丸見えになる事故**を防げます。

   一番下の **「ADVANCED CONFIGURATION」** は開かなくて大丈夫です。

   > このアプリのデータ保護は、テーブルへの公開設定ではなく **RLS（行レベルセキュリティ）** で行っています。`schema.sql` により、ログインユーザーは「自分が作ったしおりの行」だけを読み書きでき（一覧取得も自分の分だけ）、PIN の試行回数を記録する `pin_attempts` テーブルはブラウザから一切読めません（サーバー処理＝Edge Function だけがアクセス可）。そのため、上の3つの設定をどう選んでも、他人のデータが見えることはありません。

6. **「Create new project」** を押します。2分ほどで準備完了です。

> Supabase無料プランは、1アカウント（無料 Organization）につき「動いているプロジェクト」を2つまでしか持てません。既に2つある場合は、次のいずれかで枠を空けます。
> - 使っていないプロジェクトを **Pause（一時停止）**（あとで復帰できます）か、削除する
> - **Pro プランにアップグレード**する（有料）
> - このアプリ用に、**別のメールアドレス／GitHub アカウントで新しい Supabase アカウント**を作る（そのアカウントにも無料枠が2つあります。ログインの切り替え管理は自分で必要）

#### 接続に使う3つの値を控える

プロジェクトができたら、左下の **⚙（Project Settings）** を開きます。Supabase の画面は時期によって項目名が変わりますが、次の3つを探します（見当たらなければ、Project Settings 内の **「Data API」** や **「API」** 「API Keys」というページ、または左メニューの **「Integrations → Data API」** も見てください）。

1. **プロジェクトURL** … `NEXT_PUBLIC_SUPABASE_URL` に使います
   - `https://xxxxxxxxxxxx.supabase.co` の形です。
   - 「API URL」として `https://xxxx.supabase.co/rest/v1/` と表示されている場合は、**末尾の `/rest/v1/` を取り除いた `https://xxxx.supabase.co` の部分**を使ってください。
2. **公開キー（anon / Publishable key）** … `NEXT_PUBLIC_SUPABASE_ANON_KEY` に使います
   - 場所：**Project Settings（⚙）→ 左メニュー「API Keys」**（CONFIGURATION の下）。
   - このページに **「Publishable and secret API keys」** と **「Legacy anon, service_role API keys」** の2つのタブがあります。
     - 「Legacy anon, service_role API keys」タブの **`anon` `public`**（`eyJ...` で始まるとても長い文字列）
     - または「Publishable and secret API keys」タブの **Publishable key**（`sb_publishable_...`）
     - **どちらを使っても動きます。** 迷ったら `anon` `public` で構いません。
   - ※ 同じページの **`service_role`（secret）** は Row Level Security を無視できる管理者キーです。**絶対に使いません／どこにも貼りません**。
   - ※ 下部の「Disable legacy API keys（JWT-based API keys を無効化）」は**押さないでください**（`anon` キーが使えなくなります）。
3. **Reference ID**（プロジェクトID） … あとでコマンドの `<ref>` に使います
   - **Project Settings → General** に「Reference ID」として表示されます。
   - 上の1のURL `https://xxxxxxxxxxxx.supabase.co` の **`xxxxxxxxxxxx` の部分**（20文字ほど）と同じものです。

### 2-6. データベースを作る（SQLを1回貼るだけ）

1. Supabaseの左メニューから **「SQL Editor」** を開きます。
2. 「New query」などの空のエディタを開きます。
3. あなたのリポジトリの **`supabase/schema.sql`** というファイルを開き、中身をすべてコピーします。
   （リポジトリのページで `supabase` フォルダ → `schema.sql` をクリックし、「Raw」または「Copy raw file」でコピーできます）
4. SQL Editorに貼り付けて、右下の **「Run」** を押します。
5. **「Potential issue detected（destructive operations を含む）」** という確認ダイアログが出ます。**そのまま「Run query」を押して大丈夫です。**
   - このSQLには「同じ名前の設定があれば作り直す」ための `drop policy if exists ...` などが含まれるため、Supabase が念のため警告を出します。
   - 消えるのは**アクセス制御ルール（ポリシー）や制約の“古い定義”だけ**で、テーブルや保存データは削除しません（`create table if not exists` で作成します）。
   - 作りたてのプロジェクトなら中身は空なので、影響はありません。
6. 「Success. No rows returned」と出れば完了です。テーブル・アクセス制御（RLS）・権限が一括で入りました。

> このSQLは何度実行しても壊れない作りになっています。既にデータが入った状態で再実行しても、テーブルと行はそのまま、ポリシー等だけが作り直されます（うまくいかないときは、もう一度貼って実行して大丈夫です）。

### 2-7. サーバー処理（Edge Functions）を用意する

AIの呼び出しやPIN照合などのサーバー処理を、Supabaseに置きます。**APIキーはここに置かれ、利用者のブラウザには出ません。**

> **この手順だけは、パソコンで少しコマンドを打ちます**（Claude Codeは使いません）。とはいえ、コピー＆貼り付けが中心です。順番にいきましょう。

#### 2-7-1. Node.js を入れる（初回だけ）

1. <https://nodejs.org/> を開き、**「LTS」** と書かれた大きいボタンからインストーラーをダウンロードします。
2. ダウンロードしたファイルを実行し、基本的に「次へ」で進めてインストールします（設定はそのままでOK。"Add to PATH" は既定でオンです）。
3. **パソコンを再起動します**（この再起動をしないと、次のターミナルで `npx` が「認識されません」になりがちです）。
4. 再起動後、新しくターミナルを開いて `node -v` と打って Enter。`v20`（以上）と表示されれば成功です。`'node' は…認識されません` と出たら、インストールと再起動をやり直してください。

#### 2-7-2. AnthropicのAPIキーを用意する

1. <https://console.anthropic.com> を開き、アカウントを作成します。
2. 支払い方法（クレジットカード）を登録します（従量課金）。
3. 左メニューの **「API Keys」→「Create Key」（キーを作成）** を押します。
   - 途中で **「ID連携を使用すれば、APIキーは不要です」** という案内（「ID連携を設定する」／「APIキーで続ける」）が出たら、**「APIキーで続ける」** を選んでください。ID連携は GCP・AWS・GitHub Actions 用の仕組みで、このアプリ（Supabase のシークレットに固定キーを置く方式）では使えません。
4. 「APIキーを作成」の入力欄が出ます。次のように設定します。
   - **名前**：`tabi-no-shiori` など、後で分かる名前を入れます（動作には影響しません）。
   - **有効期限**：初期値が「30日間」などになっていることがあります。**「有効期限なし」にあたる選択肢**を選んでください。期限を付けると、その日を過ぎた時点で**アプリのAI機能が突然使えなくなり**、キーを作り直して Supabase に登録し直す必要が出ます。個人で長く使うなら無期限が楽です。
   - **リンクされたアカウント**：自分のアカウントのままでOK（変更不要）。
   - **スコープ／ワークスペース**：**「デフォルトワークスペース（Default）」のまま**でOK。このアプリはメッセージ生成のAPIしか使いません（「Admin APIは使用できません」と書かれていても問題ありません）。
5. **「キーを作成」** を押すと、`sk-ant-...` で始まるキーが表示されます。**コピーしてメモ帳などに貼って控えます**（この画面を閉じると二度と表示されません）。
6. 使いすぎ防止に、左メニューの **「Limits」（利用上限）** で月額の上限額を設定しておくと安心です（任意）。

#### 2-7-3. リポジトリを ZIP でダウンロードして展開する

1. あなたのリポジトリのページ（`https://github.com/あなたのユーザー名/tabi-no-shiori`）を開きます。
2. ファイル一覧の右上にある緑色の **「Code」ボタン（`<>` のアイコン）** を押します。
3. 開いたメニューの一番下の **「Download ZIP」** を押します。→ `tabi-no-shiori-main.zip` が「ダウンロード」フォルダに保存されます。
4. その ZIP ファイルを **展開（解凍）** します。
   - **Windows**：ZIP を右クリック →「すべて展開」→「展開」。
   - **Mac**：ZIP をダブルクリック。
   - `tabi-no-shiori-main` というフォルダができます（末尾が `-main` になりますが、そのままで大丈夫です）。
5. できた `tabi-no-shiori-main` フォルダを、**デスクトップなど分かりやすい場所に移動**しておきます。

#### 2-7-4. そのフォルダでターミナルを開く

**Windows の場合**

- `tabi-no-shiori-main` フォルダを開きます（中に `app` や `supabase` フォルダが見える状態）。
- Windows 11：フォルダ内の何もないところで**右クリック → 「ターミナルで開く」**。
- Windows 10：フォルダ上部の**アドレスバー（パスが表示されている帯）をクリックして文字を消し、`powershell` と入力して Enter**。
- どちらでも、そのフォルダの場所で PowerShell が開きます。

**Mac の場合**

1. 「ターミナル」アプリを開きます（`command + スペース` で `ターミナル` と検索）。
2. `cd ` と入力します（`cd` のあとに**半角スペース**）。
3. Finder から `tabi-no-shiori-main` フォルダを、ターミナルの窓の中へ**ドラッグ＆ドロップ**します（パスが自動入力されます）。
4. Enter を押します。これでそのフォルダに移動できました。

> うまく移動できたか不安なときは、`ls`（Windowsは `dir`）と打って Enter。`app` `components` `supabase` などが並んで表示されれば正しい場所です。

#### 2-7-5. ログインとキー登録

ここからは、開いたターミナルにコマンドを1行ずつ貼り付けて Enter します。`<ref>` は 2-5 で控えた Reference ID に置き換えてください（Mac / Windows 共通）。

```
npx supabase@latest login
npx supabase@latest secrets set --project-ref <ref> ANTHROPIC_API_KEY=sk-ant-ここにコピーしたキー
```

- `npx` を初めて使うと「Ok to proceed? (y)」と聞かれます。`y` を押してください。
- `login` はブラウザが開くので、承認してターミナルに戻ります。

**うまくいかないとき**

- **`npx : 用語 'npx' は…認識されません`（`CommandNotFoundException`）** … Node.js が見つかっていません。開いているターミナルをすべて閉じ、`node -v` が `v20` 以上を返すか確認します。返さない場合は 2-7-1 のインストールと**パソコン再起動**をやり直し、再起動後に**新しいターミナル**で再挑戦してください。
- **`npx : このシステムではスクリプトの実行が無効...`** … PowerShell の制限です。次を一度だけ実行し（`Y` で確定）、やり直します。
  ```
  Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
  ```

#### 2-7-6. 7つのサーバー処理をデプロイ

**Windows（PowerShell）の場合** — 次の3行をまとめて貼り付けて Enter します（`<ref>` を置き換えてから）。

```powershell
$ref = "<ref>"
"generate-plans","estimate-transit","format-itinerary","create-itinerary","reset-pin","get-shared","update-shared" |
  ForEach-Object { npx supabase@latest functions deploy $_ --project-ref $ref --no-verify-jwt }
```

**Mac の場合** — 付属のスクリプトで一括です。

```bash
bash scripts/deploy-functions.sh <ref>
```

（Windows で Git Bash を使っている人も、この `bash ...` の1行でOKです）

7つとも「Deployed Functions.」のように表示されれば成功です。1〜2分かかります。

#### 2-7-7. 確認

```
npx supabase@latest functions list --project-ref <ref>
```

`generate-plans` `estimate-transit` `format-itinerary` `create-itinerary` `reset-pin` `get-shared` `update-shared` の7つが並べば完了です。

> 途中で「Docker がどうこう」というメッセージが出ても、無視して大丈夫です（デプロイはSupabase側で行われます）。

### 2-8. ログイン方式を設定する（メール＋パスワード）

1. Supabaseの左メニュー **「Authentication」→「Sign In / Providers」** を開きます。
2. 「User Signups」という見出しの中にある **「Confirm email」** のスイッチを **OFF**（グレー）にして、**「Save changes」** を押します。

これで、メールアドレスとパスワードで登録した瞬間からログインでき、確認メールは送られません。

> なぜこの設定？　当初は「メールのリンクをタップしてログイン」という方式にしていたのですが、iPhoneでホーム画面に追加したアプリだと、リンクがSafariではなくアプリのWebView側で開かれてしまい、ログインが完了しない不具合がありました。また、Supabase無料枠のメール送信は1時間に数通の制限があり、テスト中にすぐ上限に達しました。**メール送信をまったく使わないパスワード方式**に変えることで、両方の問題が消えます。
> なお、この設定はSupabaseプロジェクト全体に効きます。同じプロジェクトを他のアプリと共用している場合は影響を確認してください。

### 2-9. GitHubに接続情報を登録して、もう一度公開する

1. 自分のリポジトリの **「Settings」→「Secrets and variables」→「Actions」** を開きます。
2. **「New repository secret」** を押し、次の2つを登録します（**Name** は下の文字を正確に。**Secret** の欄に値を貼り付けます）。1つ登録するたびに「Add secret」を押し、もう一度「New repository secret」で2つ目を登録します。

   - **Name**：`NEXT_PUBLIC_SUPABASE_URL`
     **Secret（値）**：2-5 で控えた **Project URL**（`https://xxxxxxxxxxxx.supabase.co` の形。末尾に `/rest/v1/` が付いていたら外す）
   - **Name**：`NEXT_PUBLIC_SUPABASE_ANON_KEY`
     **Secret（値）**：2-5 で控えた **公開キー**（`eyJ...` の `anon` `public`、または `sb_publishable_...`）

3. 登録できたら、**「Actions」タブ → 「Deploy to GitHub Pages」→「Run workflow」** でもう一度公開を実行します。
4. 数分後、公開URLを開き直すと、今度は本番モードで動きます（上部に「デモモード」と出なくなります）。次の 2-10 でアカウントを作って使い始めます。

> **これらの値の扱いについて**：`anon` キーは「ブラウザに埋め込んで使う前提の公開値」で、これ単体で他人のデータを読むことはできません（アクセス制御はデータベース側のRLSで行っています）。とはいえ、**必ずあなた専用のSupabaseプロジェクト**を使ってください。この記事の配布元のバックエンドを共用するものではありません。`service_role` キーやAnthropicキーは、公開される場所（リポジトリやフロントエンド）には絶対に置かないでください。

### 2-10. アプリにアカウントを作ってログインする

本番モードでは、しおりを保存・共有・履歴に残すのに**ログイン**が必要です（メールアドレスとパスワードだけ。確認メールは飛びません）。

1. 公開URL（またはホーム画面に追加したアプリ）を開きます。
2. 上部メニューの **「履歴」** を押し、**「ログイン / 新規登録」** を押します（URLを直接開く場合は `https://あなたのユーザー名.github.io/tabi-no-shiori/login/`）。
3. ログイン画面の上に **「ログイン」** と **「新規登録（はじめての方）」** の2つのタブがあります。**初めてなので「新規登録（はじめての方）」タブを選びます。**
4. **メールアドレス**と、**8文字以上のパスワード**を入力し、**「この内容で登録してはじめる」** を押します。
5. そのままログインされ、「旅行履歴」の画面に移ります。これで、しおりの作成〜保存〜PIN共有が使えます。
6. 2回目以降は、同じ画面の **「ログイン」** タブから、登録したメールアドレスとパスワードで入ります。

**うまくいかないとき**

- **`Invalid login credentials`** … メールアドレスかパスワードが違うか、まだ登録していないのに「ログイン」タブで入ろうとしています。初回は必ず「新規登録」タブから。
- **「このメールアドレスは登録済みです」** … そのメールで既にアカウントがあります（以前のお試しなど）。「ログイン」タブで入るか、Supabase の **Authentication → Users** でそのユーザーを削除してから登録し直すか、別のメールアドレスを使ってください。
- パスワードを忘れた場合は、Supabase の **Authentication → Users** で対象ユーザーを削除して、別途新規登録するのが手軽です（個人利用のため）。
- ログイン状態は「その端末のそのブラウザ」に保存されます。PC とスマホ、Safari と Chrome では、それぞれログインが必要です（履歴はアカウントにひも付くので、どの端末でも同じ内容が見えます）。

### 2-11. スマホから使う・ホーム画面に追加する

このアプリはインストール不要で、スマホのブラウザでURLを開くだけで使えます。よく使うなら、ホーム画面にアイコンを置いておくと便利です。

**iPhone（Safari）の場合**

1. **Safari** で公開URL（`https://あなたのユーザー名.github.io/tabi-no-shiori/`）を開きます。
2. 画面下の中央にある **共有ボタン**（□に上矢印）をタップします。
3. メニューを下にスクロールして **「ホーム画面に追加」** をタップします。
4. 名前（「旅のしおり」など）を確認して **「追加」** をタップします。
5. ホーム画面にアイコンができ、タップすると全画面のアプリのように開きます。

**Android（Chrome）の場合**

1. **Chrome** で公開URLを開きます。
2. 画面右上の **⋮（メニュー）** をタップします。
3. **「ホーム画面に追加」**（機種やバージョンによっては「アプリをインストール」）をタップします。
4. 名前を確認して **「追加」** をタップします。ダイアログで「自動的に追加」か、アイコンをドラッグして置きます。
5. ホーム画面にアイコンができます。タップするとブラウザで開きます（Chromeのタブとして開く機種もあります。動作はどちらでも変わりません）。

> このアプリは軽量な作りのため、Androidでは「アプリのインストール」ではなく「ショートカット追加」になる場合があります。使い勝手は同じです。ログイン状態やデモモードのデータは、その端末のそのブラウザに保存されます（iPhoneとAndroidで別々、Chromeとその他ブラウザでも別々です）。

**同行者への共有**

- 完成しおり画面に出る **共有URL** と **4桁のPIN** を伝えます。相手はスマホでもPCでも、URLを開いてPINを入れれば見られます（ログイン不要）。
- URLとPINは、できれば別々の手段で送ってください（例：URLはLINE、PINは口頭やSMS）。
- 相手も旅行中に「メモ」を書き足せます。書いた内容はお互いに反映されます。

---

## 3. 【応用】Claude Codeを使って自分好みに改造する手順

「配色を変えたい」「予算の欄がほしい」「うちの用途（合宿・出張）に言い換えたい」——そういうカスタマイズは、Claude Codeに日本語で頼むのがいちばん早いです。

### 3-1. 改造の準備

改造にはパソコンと **Node.js 20以上** が必要です。

1. **Node.jsをインストール**：<https://nodejs.org/> から「LTS」版をダウンロードしてインストールします。インストール後、ターミナルで `node -v` と打って `v20` 以上が表示されればOKです。
2. **Claude Codeをインストール**：ターミナルで次を実行します。

   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

3. **リポジトリを自分のパソコンに取得**します（`あなた` は自分のGitHubユーザー名に置き換え）。

   ```bash
   git clone https://github.com/あなた/tabi-no-shiori.git
   cd tabi-no-shiori
   npm install
   ```

4. **プレビュー起動**：

   ```bash
   npm run dev
   ```

   `http://localhost:3000` をブラウザで開くと、手元でアプリを確認できます（Supabase未設定でも、AI提案はサンプルになりますが全画面のレイアウトと操作は確認できます。デザイン調整の確認はこれで十分です）。別のターミナルを開いて次に進みます。

5. **Claude Codeを起動**：`tabi-no-shiori` フォルダで次を実行します。

   ```bash
   claude
   ```

   初回はブラウザでのログイン（Anthropicアカウント）を求められます。案内に従って認証すると、対話が始まります。あとは日本語で指示するだけです。

### 3-2. デザイン・UIをカスタマイズするプロンプト例

Claude Codeに、そのまま貼って使える例です。

- 「表紙とヘッダーの配色を、ネイビーから深緑（#1f3d33 くらい）に変えて。金のアクセントはそのままで。」
- 「タイムラインの縦ラインとドットをもう少し大きく・濃くして、手帳っぽさを強めて。」
- 「トップの入力画面の見出しを『家族旅行のしおりをつくる』に変えて、説明文も家族向けの言い回しにして。」
- 「完成しおり画面を、印刷したときにA4で1日1ページになるレイアウトにして。画面表示は今のままでいい。」
- 「スマホで文字が少し小さいので、本文を1pxだけ大きくして、行間も気持ち広げて。」

指示のあと、Claude Codeが変更したら `npm run dev` のプレビューで確認し、良ければ「これでいい、コミットして」と伝えます。

### 3-3. 新機能を追加・調整するプロンプト例

- 「各スポットに『予算（円）』の入力欄を追加して。完成しおりの各日の下に、その日の合計金額を表示して。」
- 「入力画面に『交通手段（新幹線／飛行機／車）』の選択を追加して、AIプラン生成のプロンプトにも渡して、移動の前提にしてもらって。」
- 「しおりに『チェックリスト』セクションを追加。項目を自由に足せて、チェックすると打ち消し線がつくようにして。共有相手も編集できるように。」
- 「『旅のしおり』という言葉を全部『合宿のしおり』に置き換えて。プラン生成のテーマも、観光ではなく合宿（練習・ミーティング・食事）向けにして。」
- 「AIのプラン生成で、雨の日プランも1案入れてもらうようにプロンプトを調整して。」

> データベースの列を増やす変更（予算欄など）を頼むと、Claude Codeは `supabase/schema.sql` も更新します。その場合は、更新後のSQLをSupabaseのSQL Editorでもう一度実行する必要があります。Claude Codeに「Supabase側で何をすればいい？」と聞けば教えてくれます。

#### 例：移動時間を「AIの概算」から「実際の経路検索」に置き換える

標準の「✦ AIで所要時間を見積もる」は、Claudeの地理知識による概算で、正確さには限界があります。地図サービスの経路検索APIとつなぐと、実際の道路・路線に基づいた所要時間が出せます。

まず、経路検索APIのキーを用意します。どちらか一方でかまいません。

- **OpenRouteService**（<https://openrouteservice.org/>）… 無料枠が大きく（1日あたり2,000リクエスト程度）、クレジットカード登録なしで始められます。徒歩・自転車・車に対応。まずはこちらがおすすめです。
- **Google Maps Platform**（Routes API）… 精度が高く公共交通機関にも対応しますが、利用には請求先アカウント（クレジットカード）の登録が必要です。毎月の無料クレジットあり。

次に、Claude Codeへこう頼みます（ORSの例。キーは伏せて伝えてかまいません）。

```
移動ブロックの所要時間を、AIの概算ではなく OpenRouteService の
経路検索APIで出すようにしたい。次の方針で実装して：

- 新しい Supabase Edge Function `route-time` を追加。ORS の API キーは
  supabase の secrets（ORS_API_KEY）から読む。ブラウザには出さない。
- 入力は「出発スポット名」「目的スポット名」「エリア（都道府県・市区町村）」
  「移動手段（徒歩/自転車/車）」。まず ORS のジオコーディングで各地点の
  緯度経度を求め、続けて directions API で所要時間を取得する。
- 地点が特定できない・APIが失敗したときは、今の estimate-transit（AI概算）に
  フォールバックする。
- フロントの「✦ AIで所要時間を見積もる」ボタンは、まず route-time を試して、
  だめなら従来どおり AI概算、という順で動かす。結果の下に「経路検索」か
  「AI概算」かが分かる小さなラベルを出して。
- README と supabase/config.toml、scripts/deploy-functions.sh も更新して。
```

実装後の作業：

1. `npx supabase@latest secrets set --project-ref <ref> ORS_API_KEY=（取得したキー）`
2. `npx supabase@latest functions deploy route-time --project-ref <ref> --no-verify-jwt`（`scripts/deploy-functions.sh` に追記してもらった場合は、もう一度そのスクリプトを実行）
3. `git push` でフロント側も反映

> 公共交通機関（電車・バス）の正確な所要時間は、ORS では出せません（車・徒歩・自転車が中心）。電車移動の精度まで求めるなら Google Routes API を使い、上のプロンプトの「OpenRouteService」を「Google Routes API」に、`ORS_API_KEY` を `GOOGLE_MAPS_API_KEY` に読み替えてClaude Codeに頼んでください。いずれの場合も **APIキーは必ず Edge Function の secrets に置き**、`NEXT_PUBLIC_` の環境変数やフロントのコードには絶対に書かないでください（3-4参照）。

### 3-4. もっと手軽に使ってもらうための応用と、その注意

このアプリは、SupabaseのURLと `anon` キーを **GitHubのSecrets** に入れておき、公開時にビルドへ埋め込む方式です。`anon` キーは公開前提の値なので、この方式で問題ありません（データの保護はデータベースのRLSが担当）。

一方で、次は**絶対にやらないでください**。

- `service_role` キーや `ANTHROPIC_API_KEY` を、フロントエンドのコードやリポジトリに直接書く
- それらを `NEXT_PUBLIC_` で始まる名前の環境変数に入れる（`NEXT_PUBLIC_` は公開される前提です）

Publicリポジトリでは、Secretsに入れた値以外（＝コードに書いた値）は誰でも読めます。また、Secretsに入れた `anon` キーも、最終的には公開ページのJavaScriptに含まれるため「隠し通せる秘密」ではありません。**あくまで自分専用のSupabaseプロジェクトを使う**、という前提を守ってください。

### 3-5. 変更をGitHub Pagesに反映する

改造してコミットしたら、GitHubに送るだけです。

```bash
git push
```

`main` ブランチにpushされると、GitHub Actionsが自動でビルドし直し、数分後に公開URLへ反映されます（進み具合はリポジトリの「Actions」タブで見られます）。

> **改造時のハマりどころ**
> - `npm run dev`（プレビュー）を動かしたまま `npm run build` すると、内部のキャッシュ（`.next` フォルダ）が壊れて500エラーが出ることがあります。ビルドやビルド確認は、devを止めてから行ってください。
> - Windowsの Git Bash では、`NEXT_PUBLIC_BASE_PATH=/tabi-no-shiori` のような「スラッシュ始まりの値」をパスと勘違いして書き換えることがあります。手元で試すときは値の前に `MSYS_NO_PATHCONV=1` を付けます（GitHub Actions側では不要）。
> - このアプリはURLのクエリ（`?id=...`）を読む画面がいくつかあり、Next.jsの静的書き出しでは `useSearchParams` を `<Suspense>` で囲む必要があります。Claude Codeは基本的に守って書きますが、ビルドエラーが出たらこの点を伝えると直してくれます。

---

## 4. よくあるエラーとトラブルシューティング

### GitHub Pagesが反映されない・404になる

- **「Actions」タブ**を開き、最新の実行が緑チェックか確認します。赤×なら、その実行をクリックしてエラー内容を確認します。
- **Settings → Pages** の Source が **「GitHub Actions」** になっているか確認します。
- **Settings → Actions → General → Workflow permissions** が **「Read and write permissions」** か確認します（初回公開に必須）。
- URL末尾のスラッシュを確認します。`https://ユーザー名.github.io/tabi-no-shiori/`（最後の `/` まで）で開きます。
- 反映には数分かかります。ブラウザは「強制再読み込み」（PCは Ctrl+Shift+R、スマホは一度タブを閉じて開き直す）を。

### スマホでうまく動かない・ログインできない

- 古い画面が残っている場合は、**強制再読み込み**を試します。iPhone（ホーム画面アプリ）はアプリスイッチャーから一度スワイプで終了→再起動、Androidや通常のブラウザタブは、そのタブを閉じてURLから開き直します。
- 画面が崩れる・ボタンが反応しない場合は、ブラウザのキャッシュをクリアするか、シークレット/プライベートウィンドウで開いて切り分けます。
- ログインは **メールアドレス＋パスワード** 方式です。`/login` 画面で **「新規登録（はじめての方）」タブ** を選んでから登録してください（「ログイン」タブのまま登録しようとすると `Invalid login credentials` になります）。
- 「このメールアドレスは登録済みです」と出る場合は、以前のテストで作ったアカウントが残っています。Supabaseの **Authentication → Users** から削除するか、別のメールアドレスで登録してください。
- しおりを保存したのに「しおりが見つかりませんでした」と出る場合は、`supabase/schema.sql` の実行が中途半端な可能性があります。SQL Editorでもう一度貼って実行してみてください（このSQLは何度実行しても安全です）。

### AIの提案が出ない／「デモモードです」と表示される

- GitHubの **Secrets**（`NEXT_PUBLIC_SUPABASE_URL` と `NEXT_PUBLIC_SUPABASE_ANON_KEY`）を登録したあと、**もう一度「Run workflow」で公開し直した**か確認します。Secretsは公開時にビルドへ埋め込まれるため、登録しただけでは反映されません。
- Supabaseの **Edge Functions** が7つデプロイされているか、`npx supabase@latest functions list --project-ref <ref>` で確認します。
- AnthropicのAPIキーが登録されているか、コンソールで支払い方法が有効か確認します。

### AIの移動時間の見積もりがずれる

このアプリの「AIで所要時間を見積もる」は、経路検索エンジンではなくClaudeの知識による概算です。有名スポット同士は比較的正確ですが、マイナーな地点や住所指定ではずれます。大きくずれたら手入力で上書きしてください。正確さが必要なら、**3-3の「移動時間を『AIの概算』から『実際の経路検索』に置き換える」** の手順で、地図サービスの経路検索APIに差し替えられます。

### Claude Codeでエラーが出たとき

- エラーメッセージをそのままClaude Codeに貼り付けて「これを直して」と伝えます。たいていその場で修正まで進みます。
- `node -v` で 20 以上か確認します。古ければNode.jsを入れ直します。
- 依存関係が壊れたら、`tabi-no-shiori` フォルダで `rm -rf node_modules && npm install` をやり直します。
- 「型チェックだけ先に通したい」ときは `npx tsc --noEmit` が使えます。

---

## 5. 免責事項・注意事項

- このアプリおよび本記事は、**動作を保証するものではありません**。GitHub・Supabase・Anthropic各サービスの仕様変更により、手順どおりに動かなくなる可能性があります。画面の項目名やボタン名も、各サービスのアップデートで変わることがあります。実際の画面と食い違う場合は、近い名前の項目を探してください。
- **個別のサポートは行っておりません。** うまくいかない場合は、エラーメッセージをClaude CodeやAIに相談しながら進めてください。
- 利用・カスタマイズは**すべて自己責任**でお願いします。特に、`service_role` キーやAPIキーの取り扱い（公開場所に置かない）にはご注意ください。
- **データのバックアップについて**：Supabaseの無料プランには、自動バックアップからの復元機能がありません。誤って消したしおりは戻せません。大事な旅程は、Supabaseの **Table Editor** から各テーブル（`itineraries` `days` `spots` `transits`）をCSVでエクスポートしておくか、完成しおり画面をスクリーンショット／印刷して控えておくことをおすすめします。
- Anthropic APIは**従量課金**です。想定外の高額利用を防ぐため、Anthropicコンソールの **Usage Limits**（利用上限）を設定しておくと安心です。
